# Core (required)
langgraph>=0.2.28
langchain-core>=0.3.0

# Optional: enables LLM-polished narrative when OPENAI_API_KEY is set
# langchain-openai>=0.2.0

# Optional: faster/robuster TF-IDF for RAG (a pure-Python fallback is built in)
# scikit-learn>=1.3.0

# Optional: nicer CLI output
# rich>=13.0.0
