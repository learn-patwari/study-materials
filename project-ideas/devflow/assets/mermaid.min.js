/* Placeholder — replace with the real mermaid.min.js from https://cdn.jsdelivr.net/npm/mermaid/dist/mermaid.min.js */
window.mermaid = {
  initialize: function() {},
  run: function() {
    document.querySelectorAll('.mermaid').forEach(function(el) {
      el.textContent = '(mermaid.min.js not installed — download from https://cdn.jsdelivr.net/npm/mermaid/dist/mermaid.min.js and place in assets/)';
    });
  }
};
